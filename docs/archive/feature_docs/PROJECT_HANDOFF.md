# 프로젝트 인수인계 문서

**작성일:** 2026-01-02
**프로젝트명:** n8n-test (호러 소설 생성기 n8n 연동)
**현재 단계:** 1단계 완료 (Task 1~4)

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [작업 범위 및 제약사항](#작업-범위-및-제약사항)
3. [완료된 작업 (1단계)](#완료된-작업-1단계)
4. [남은 작업 (2단계, 3단계)](#남은-작업-2단계-3단계)
5. [중요한 설계 결정 사항](#중요한-설계-결정-사항)
6. [파일 구조 및 위치](#파일-구조-및-위치)
7. [다음 작업 시 주의사항](#다음-작업-시-주의사항)
8. [참고 정보](#참고-정보)

---

## 프로젝트 개요

### 목적

**이미 개발된 Python 기반 Claude API 호러 소설 생성기를 n8n 워크플로와 연동하는 방법을 문서화하고 가이드를 제공하는 것**

### 핵심 원칙

❌ **이 레포지토리는 다음을 다루지 않음:**
- 콘텐츠 생성 로직 개선
- 프롬프트 설계 또는 품질 향상
- Claude 모델 변경
- 비즈니스 로직 추가

✅ **이 레포지토리는 다음만 다룸:**
- n8n과 기존 Python 코드의 연동 방법
- 운영 가이드 및 워크플로 템플릿
- 환경 설정 및 보안

### 역할 정의

**시니어 엔지니어 역할:**
- 자동화 파이프라인과 API 연동 전문
- n8n 워크플로 설계 관점
- 기존 코드를 "있는 그대로" 연동

---

## 작업 범위 및 제약사항

### 절대적인 작업 순서 규칙

1. **현황 파악 우선**
   - 기존 코드 구조 분석
   - 입/출력 인터페이스 확인
   - 파일 기반 저장 구조 파악

2. **Task List 작성 및 컨펌**
   - 분석 완료 후 Task List 제시
   - 사용자 컨펌 받기 전 구현 금지

3. **단계별 진행**
   - 각 Task 완료 시 간단한 요약 제공
   - 다음 Task로 자동 진행 금지

### 범위 제한

**절대 하지 말 것:**
- Python 코드의 콘텐츠 생성 로직 수정
- 프롬프트 템플릿 내용 변경 (예시만 제공 가능)
- 새로운 기능 개발
- Claude API 호출 방식 변경

**해야 할 것:**
- n8n 연동 문서 작성
- 워크플로 템플릿 제공
- 환경 설정 가이드 작성
- 운영 패턴 문서화

---

## 완료된 작업 (1단계)

### Task 1: Execute Command 방식 기본 연동 가이드 ✅

**파일:** `docs/n8n_execute_command_guide.md`

**주요 내용:**
- Execute Command 노드 설정 방법
- 4가지 실행 패턴 (기본/템플릿 선택/n8n 변수/가상환경)
- 템플릿 선택 워크플로 3가지 예시
- Docker 환경 고려사항
- 문제 해결 가이드

**중요 반영 사항:**
- ✅ 다중 템플릿 구조 지원
- ✅ Python -c 옵션으로 템플릿 선택
- ✅ n8n 변수 `{{ $json.template_path }}` 활용
- ✅ templates 디렉토리 구조 설명

---

### Task 2: 출력 완결성 검증 및 에러 처리 워크플로 패턴 ✅

**파일:** `docs/n8n_output_validation.md`

**주요 내용:**
- metadata 기반 검증 (word_count, output_tokens, title)
- Markdown 구조 검증 (문장 끝, 코드 블록, frontmatter)
- YAML frontmatter 파싱
- n8n If 노드 분기 패턴
- Loop 노드 재시도 로직
- Slack/Email 알림 및 로그 수집
- [참고용] 프롬프트 레벨 완결성 제약 예시

**중요 반영 사항:**
- ✅ 템플릿별 출력 특성 차이 명시
- ✅ 검증 기준을 템플릿에 종속시키지 않는 방식
- ✅ 재시도 시 다른 템플릿 선택 가능
- ✅ Claude/프롬프트 품질 문제로 접근 안 함

**범위 준수:**
- ❌ 프롬프트 자체 수정 안 함
- ✅ n8n 워크플로 검증 방법만 제공
- ✅ 프롬프트 예시는 "참고용" 섹션으로 분리

---

### Task 3: n8n 기본 워크플로 템플릿 ✅

**파일:**
1. `n8n_workflows/01_basic_generation.json`
2. `n8n_workflows/02_template_selection.json`
3. `docs/n8n_workflow_import_guide.md`

**워크플로 1 (Basic):**
- Manual Trigger → Execute Command → 파일 읽기 → 파싱
- 기본 템플릿 사용 (`main.py` 실행)
- 단순 구조, 분기 없음

**워크플로 2 (Template Selection):**
- Set 노드로 템플릿 선택
- n8n 변수로 동적 템플릿 전달
- 기본 검증 로직 포함
- 검증 결과 출력 (`is_complete`, `validation_message`)

**Import 가이드:**
- Import 방법 (파일/JSON)
- 필수 수정 사항 (경로 변경)
- 템플릿 선택 예시
- 문제 해결

**중요 반영 사항:**
- ✅ Set 노드에서 `template_path` 변수 설정
- ✅ 4가지 템플릿 옵션 제시
- ✅ Webhook으로 템플릿 입력 받는 방법 설명

**범위 준수:**
- ✅ 최소 동작 예제 (단순 구조)
- ❌ 고급 분기/배치/스케줄링 로직 없음
- ❌ 실패 시 중단 (재시도는 Task 2 참고)

---

### Task 4: 환경 설정 및 보안 가이드 ✅

**파일:** `docs/n8n_environment_setup.md`

**주요 내용:**
- 프로젝트 디렉토리 구조 (templates 포함)
- 로컬 n8n 환경 설정
- Docker n8n 환경 설정 (볼륨 마운트, Custom Image)
- n8n.cloud 대안 (HTTP API)
- 보안 체크리스트
- templates 디렉토리 관리 (추가, 백업, 검증)
- 문제 해결

**중요 반영 사항:**
- ✅ templates 디렉토리 구조 명시
- ✅ Docker 볼륨 마운트로 templates 공유
- ✅ 템플릿 추가 방법 및 검증
- ✅ 템플릿 경로 검증 (Command Injection 방지)

**범위 준수:**
- ✅ n8n 연동 관점만
- ❌ API 키 로테이션, 고급 보안 제외
- ✅ 실무 사용 가능한 기본 설정

---

## 남은 작업 (2단계, 3단계)

### 2단계 (권장 Task) - 별도 컨펌 필요

#### Task 5: 출력 파일 활용 워크플로 패턴 문서화

**해결할 문제:**
- 생성된 마크다운/JSON 파일을 n8n에서 후속 처리
- 블로그 자동 게시, 클라우드 저장 등

**산출물:**
- `docs/n8n_output_usage_patterns.md`
  - Read Binary File 활용
  - 블로그/CMS 자동 업로드 (Ghost, WordPress, Notion)
  - 클라우드 스토리지 (S3, Google Drive)
  - Git 자동 커밋/푸시 (Astro 블로그)

**범위:**
- ❌ 파일 포맷/생성 로직 수정 안 함
- ✅ n8n에서 활용 예시만

---

#### Task 6: 배치 생성 및 스케줄링 워크플로 패턴 문서화

**해결할 문제:**
- 여러 테마 순차 생성
- cron 스케줄링으로 정기 생성

**산출물:**
- `docs/n8n_batch_scheduling.md`
  - Loop 노드 활용 배치 생성
  - Cron Trigger 설정
  - 결과 집계 워크플로

**범위:**
- ❌ 배치 로직/생성 품질 수정 안 함
- ✅ n8n 반복 호출 패턴만

---

### 3단계 (선택/레퍼런스 Task) - 별도 컨펌 필요

#### Task 7: HTTP API 래퍼 구현 가이드 (선택사항)

**해결할 문제:**
- Execute Command가 아닌 HTTP Request 노드 연동
- 비동기 처리, 진행 상태 조회

**산출물:**
- `api_server.py` (FastAPI 래퍼)
- `docs/n8n_api_integration.md`

**범위:**
- ✅ 기존 코드를 HTTP로 노출만
- ❌ 콘텐츠 생성 로직 수정 안 함
- ⚠️ 초기 연동은 Execute Command 권장

---

#### Task 8: n8n 연동 옵션 비교 문서 (레퍼런스)

**산출물:**
- `docs/n8n_integration_comparison.md`
- Execute Command vs HTTP API vs Code 노드 비교

---

#### Task 9: 트러블슈팅 가이드 (레퍼런스)

**산출물:**
- `docs/n8n_troubleshooting.md`
- 자주 발생하는 오류 및 해결 방법

---

## 중요한 설계 결정 사항

### 1. 다중 템플릿 구조

**현재 상태:**
- `templates/` 디렉토리에 여러 JSON 템플릿 파일 존재
- `generate_horror_story(template_path=...)` 파라미터로 선택 가능

**영향받는 Task:**
- Task 1: 템플릿 선택 4가지 패턴 제시
- Task 2: 템플릿별 검증 기준 동적 조정
- Task 3: Set 노드로 템플릿 변수 설정
- Task 4: templates 디렉토리 마운트/관리

**중요:**
- 템플릿 경로는 `templates/파일명.json` 형식
- n8n 변수: `{{ $json.template_path }}`
- 검증은 템플릿에 종속되지 않는 방식

---

### 2. 출력 완결성 이슈 처리 방침

**문제:**
- Claude API가 가끔 불완전한 출력 생성 (중간 종료)
- 이는 코드 결함이 아니라 출력 제약

**처리 방침:**
- ❌ Claude 모델/프롬프트 품질 문제로 접근 안 함
- ✅ n8n 워크플로에서 검증 및 재시도
- ✅ 프롬프트 개선은 "참고용 예시"만 제공

**구현:**
- metadata 기반 검증 (word_count, output_tokens)
- Markdown 구조 검증
- Loop 노드 재시도 (최대 3회)
- 실패 시 알림

---

### 3. 워크플로 설계 원칙

**1단계 워크플로 특징:**
- 최소 동작 예제 중심
- 단순 구조 (고급 분기 없음)
- 실패 시 중단 (재시도는 Task 2 참고)

**이유:**
- 사용자가 쉽게 이해하고 수정 가능
- 복잡한 로직은 문서 참고하여 확장

---

## 파일 구조 및 위치

```
n8n-test/
├── docs/                                  # 문서 (1단계 완료)
│   ├── PROJECT_HANDOFF.md                 # 이 문서
│   ├── n8n_execute_command_guide.md       # Task 1
│   ├── n8n_output_validation.md           # Task 2
│   ├── n8n_workflow_import_guide.md       # Task 3
│   └── n8n_environment_setup.md           # Task 4
│
├── n8n_workflows/                         # 워크플로 템플릿 (1단계 완료)
│   ├── 01_basic_generation.json           # 기본 생성
│   └── 02_template_selection.json         # 템플릿 선택
│
├── templates/                             # 프롬프트 템플릿
│   ├── horror_story_prompt_template.json  # 기본
│   ├── sample.json
│   └── (추가 템플릿 파일들...)
│
├── generated_stories/                     # 생성된 소설
│   ├── horror_story_*.md
│   └── horror_story_*_metadata.json
│
├── logs/                                  # 실행 로그
│   └── horror_story_*.log
│
├── horror_story_generator.py              # 핵심 모듈 (변경 없음)
├── main.py                                # 테스트 스크립트 (변경 없음)
├── .env                                   # 환경 변수 (사용자가 설정)
├── .env.example                           # 환경 변수 예시
├── pyproject.toml                         # Poetry 의존성
└── README.md                              # 프로젝트 개요
```

---

## 다음 작업 시 주의사항

### 컨펌 프로세스 준수

**반드시 지켜야 할 규칙:**
1. Task List 제시 후 사용자 컨펌 받기
2. 컨펌 없이 코드/파일 작성 금지
3. 각 Task 완료 후 요약 제공
4. 다음 Task 자동 진행 금지 (사용자 지시 대기)

### 2단계 시작 전 확인사항

**1단계 검토:**
- [ ] 사용자가 1단계 문서 검토 완료
- [ ] 실제 n8n 환경에서 테스트 완료 여부 확인
- [ ] 수정 요청사항 반영

**2단계 컨펌 요청:**
- Task 5, 6의 필요성 확인
- 우선순위 조정 필요 여부
- 새로운 요구사항 확인

### 범위 이탈 방지

**자주 발생하는 범위 이탈:**
- ❌ 프롬프트 내용을 직접 수정하려는 시도
- ❌ Python 코드의 생성 로직 개선 제안
- ❌ 새로운 기능 추가 (예: 이미지 생성, 다국어 지원)

**올바른 접근:**
- ✅ "기존 코드를 있는 그대로" n8n에서 호출
- ✅ n8n 워크플로로 할 수 있는 것만 제안
- ✅ 문서와 가이드만 제공

### 템플릿 관련 작업 시

**주의사항:**
- 템플릿 파일 내용은 수정하지 않음
- 새 템플릿 추가 방법만 안내
- 템플릿 선택 방법 문서화

**허용:**
- 템플릿 추가/삭제 방법 설명
- 템플릿 검증 방법 안내
- 템플릿별 검증 기준 조정 방법

---

## 참고 정보

### 기존 코드 주요 함수

```python
# 전체 파이프라인
generate_horror_story(
    template_path: str = "horror_story_prompt_template.json",
    custom_request: Optional[str] = None,
    save_output: bool = True
) -> Dict[str, Any]

# 반환값 구조
{
    "story": str,              # 생성된 소설
    "metadata": {
        "generated_at": str,
        "model": str,
        "template_used": str,
        "word_count": int,
        "usage": {             # 토큰 사용량
            "input_tokens": int,
            "output_tokens": int,
            "total_tokens": int
        },
        "title": str,
        "tags": List[str]
    },
    "file_path": str          # 저장된 파일 경로
}
```

### 환경 변수

```env
ANTHROPIC_API_KEY=sk-ant-xxxxx  # 필수
CLAUDE_MODEL=claude-sonnet-4-5-20250929
MAX_TOKENS=8192
TEMPERATURE=0.8
OUTPUT_DIR=./generated_stories
LOG_LEVEL=INFO
```

### 의존성

```toml
python = ">=3.14"
python-dotenv = ">=1.2.1,<2.0.0"
anthropic = ">=0.40.0,<1.0.0"
```

---

## 인수인계 체크리스트

### 현황 파악

- [x] 프로젝트 목적 이해
- [x] 작업 범위 및 제약사항 숙지
- [x] 1단계 완료 내용 확인
- [x] 기존 코드 구조 파악
- [x] 다중 템플릿 구조 이해

### 문서 확인

- [x] `docs/n8n_execute_command_guide.md`
- [x] `docs/n8n_output_validation.md`
- [x] `docs/n8n_workflow_import_guide.md`
- [x] `docs/n8n_environment_setup.md`
- [x] `docs/PROJECT_HANDOFF.md` (이 문서)

### 워크플로 확인

- [x] `n8n_workflows/01_basic_generation.json`
- [x] `n8n_workflows/02_template_selection.json`

### 다음 작업 준비

- [ ] 2단계 필요성 확인
- [ ] Task 5, 6 우선순위 협의
- [ ] 새로운 요구사항 수집
- [ ] 1단계 피드백 반영

---

## 연락처 및 이슈

**새 채팅 시작 시 제공할 정보:**
1. 이 문서 (`docs/PROJECT_HANDOFF.md`) 참조
2. 현재 완료 단계: "1단계 완료"
3. 진행 요청: "2단계부터 시작" 또는 "1단계 검토 후 수정"

**이슈 발생 시:**
- 각 Task 문서의 "문제 해결" 섹션 참고
- 범위 이탈 의심 시 이 문서의 "범위 제한" 섹션 재확인

---

## 버전 히스토리

| 버전 | 날짜 | 내용 |
|------|------|------|
| 1.0 | 2026-01-02 | 1단계 완료, 인수인계 문서 작성 |

---

**이 문서는 새 채팅에서 작업을 이어갈 때 필수적으로 참조해야 합니다.**
