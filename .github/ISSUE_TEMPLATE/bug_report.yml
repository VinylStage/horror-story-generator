name: 🐛 버그 리포트
description: 버그를 발견하셨나요? 재현 가능한 문제를 신고해주세요.
title: "[Bug] "
labels: ["type: bug"]
body:
  - type: markdown
    attributes:
      value: |
        버그 리포트를 작성해주셔서 감사합니다.
        가능한 한 상세하게 작성해주시면 빠른 해결에 도움이 됩니다.

  - type: textarea
    id: description
    attributes:
      label: 버그 설명
      description: 어떤 문제가 발생했는지 명확하게 설명해주세요.
      placeholder: 예) 스토리 생성 API 호출 시 500 에러가 발생합니다.
    validations:
      required: true

  - type: textarea
    id: reproduce
    attributes:
      label: 재현 단계
      description: 버그를 재현할 수 있는 단계를 순서대로 작성해주세요.
      placeholder: |
        1. '/api/v1/story/generate' 엔드포인트 호출
        2. 특정 파라미터 전달
        3. 에러 발생 확인
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: 예상 동작
      description: 정상적으로 동작했을 때 어떻게 되어야 하는지 설명해주세요.
      placeholder: 예) 공포 스토리가 정상적으로 생성되어야 합니다.
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: 실제 동작
      description: 실제로 어떤 일이 발생하는지 설명해주세요.
      placeholder: 예) Internal Server Error (500)가 반환됩니다.
    validations:
      required: true

  - type: textarea
    id: environment
    attributes:
      label: 환경 정보
      description: 문제가 발생한 환경 정보를 입력해주세요.
      value: |
        - OS: [예: Ubuntu 22.04, macOS 14.0, Windows 11]
        - Python Version: [예: 3.11]
        - AI Provider: [예: OpenAI, Google AI]
    validations:
      required: true

  - type: textarea
    id: logs
    attributes:
      label: 로그 및 에러 메시지
      description: 관련된 로그나 에러 메시지가 있다면 붙여넣어주세요.
      render: shell
      placeholder: |
        에러 스택 트레이스나 로그를 여기에 붙여넣어주세요.

  - type: textarea
    id: additional
    attributes:
      label: 추가 정보
      description: 스크린샷이나 기타 참고할 만한 정보가 있다면 추가해주세요.
