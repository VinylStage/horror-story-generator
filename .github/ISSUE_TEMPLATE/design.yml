name: 🎨 설계 / RFC
description: 아키텍처 설계나 기술적 의사결정을 논의합니다.
title: "[Design] "
labels: ["type: design"]
body:
  - type: markdown
    attributes:
      value: |
        설계 논의나 RFC(Request for Comments)를 작성해주세요.
        중요한 기술적 결정은 팀원들과 충분히 논의한 후 진행하는 것이 좋습니다.

  - type: textarea
    id: background
    attributes:
      label: 배경 및 문제 상황
      description: 왜 이 설계 논의가 필요한가요?
      placeholder: |
        예) 현재 스토리 생성 로직이 단일 AI 프로바이더에 의존하여 확장성이 제한됩니다.
    validations:
      required: true

  - type: textarea
    id: proposal
    attributes:
      label: 제안하는 설계
      description: 구체적인 설계안을 설명해주세요.
      placeholder: |
        예)
        - 현재: 단일 AI 프로바이더 직접 호출
        - 제안: 프로바이더 추상화 레이어 도입
        - 이유: 다양한 AI 모델 지원 및 폴백 전략 구현 필요
    validations:
      required: true

  - type: textarea
    id: alternatives
    attributes:
      label: 대안 방안
      description: 다른 접근 방법도 검토하셨나요?
      placeholder: |
        예)
        Option A: 팩토리 패턴 사용 (장점: 간단, 단점: 런타임 변경 어려움)
        Option B: DI 컨테이너 활용 (장점: 유연성, 단점: 복잡도 증가)
    validations:
      required: true

  - type: textarea
    id: impact
    attributes:
      label: 영향 범위
      description: 이 설계 변경이 시스템에 어떤 영향을 미치나요?
      placeholder: |
        예)
        - 변경 필요 모듈: model_provider.py, story_generator.py
        - 기존 API 호환성: Maintained
    validations:
      required: true

  - type: textarea
    id: tradeoffs
    attributes:
      label: 트레이드오프
      description: 이 설계의 장단점을 명시해주세요.
      placeholder: |
        장점:
        - 다양한 AI 프로바이더 지원
        - 폴백 전략 구현 가능

        단점:
        - 추상화 레이어로 인한 복잡도 증가
        - 초기 구현 비용

  - type: textarea
    id: implementation
    attributes:
      label: 구현 계획 (선택사항)
      description: 단계별 구현 계획이 있다면 작성해주세요.
      placeholder: |
        Phase 1: 프로바이더 인터페이스 정의
        Phase 2: 기존 코드 리팩토링
        Phase 3: 추가 프로바이더 구현

  - type: textarea
    id: references
    attributes:
      label: 참고 자료
      description: 관련 문서, 논문, 벤치마크 결과 등을 첨부해주세요.
